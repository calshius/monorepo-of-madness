# PostgreSQL Configuration for RAG Database
# Copy this file to .env and modify as needed

# Database connection settings
POSTGRES_DB=rag_database
POSTGRES_USER=rag_user
POSTGRES_PASSWORD=rag_password
POSTGRES_PORT=5432

# Optional: If you want to connect from outside Docker
# POSTGRES_HOST=localhost

# Vector embedding dimensions (adjust based on your embedding model)
# Google Gemini text-embedding-004: 768
# OpenAI text-embedding-ada-002: 1536 (deprecated)
# Sentence Transformers all-MiniLM-L6-v2: 384
# Adjust the schema if using different dimensions
EMBEDDING_DIMENSION=768